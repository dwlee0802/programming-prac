https://www.acmicpc.net/problem/28278

notes
- only need to surroud switch cases with {} if you declare a variable
- std::cin stops at the first whitespace
- dynamic alloc recap. `int *array = new int[cmdcount];`

```
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
```
for fast i/o